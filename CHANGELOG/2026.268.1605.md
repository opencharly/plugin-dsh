# 2026.268.1605 — feat(plugin-dsh): adopt spec 261 (schema-head pin bump)

## Summary

The schema-249→261 wave leg for `plugin-dsh` (issue `charly#633`) — the **compiled-in** `command:dsh`/`verb:dsh` plugin. Bump the compiled `sdk`/`spec` pins so a 261 charly can resolve a 261 project through it (the plugin self-loads project config via `sdk/loaderkit`, whose `GateSchemaVersion` rejects a config NEWER than its compiled spec HEAD).

**The 2-file diff, every file accounted for (`git diff --stat origin/main...HEAD` = 30 insertions, 30 deletions):**

| file | change |
|---|---|
| `candy/plugin-dsh/go.mod` | direct: `sdk v0.2026234.347 → v0.2026266.623`, `spec v0.2026232.520 → v0.2026266.545`. Indirect bumps pulled by the new sdk/spec: `golang.org/x/term v0.43.0 → v0.45.0`, `x/net v0.55.0 → v0.57.0`, `x/sys v0.45.0 → v0.47.0`, `x/text v0.37.0 → v0.40.0`. |
| `candy/plugin-dsh/go.sum` | the matching go.mod + zip hashes for the above; the graph-only `x/crypto|mod|sync|tools` bumps; **and the cross-repo relocation of the two transitive `deploykit` requires** (below). |

**The `go.sum` relocation (block-1 accounting).** Two lines change repo identity because the new `sdk` moved them in its OWN `go.mod` — this plugin does not import them directly; they are transitive requires of `github.com/opencharly/sdk/deploykit`. Proven by diffing the SDK module's own manifest across the two pins:

```
$ grep -E 'plugin-(egress|init)' $(go env GOMODCACHE)/github.com/opencharly/sdk@v0.2026234.347/go.mod
13:	github.com/opencharly/charly/candy/plugin-egress v0.0.0-20260806230842-5cda00cb0fbc
14:	github.com/opencharly/charly/candy/plugin-init   v0.0.0-20260806230842-5cda00cb0fbc

$ grep -E 'plugin-(egress|init)' $(go env GOMODCACHE)/github.com/opencharly/sdk@v0.2026266.623/go.mod
14:	github.com/opencharly/plugin-egress/candy/plugin-egress v0.2026237.1419
15:	github.com/opencharly/plugin-init/candy/plugin-init     v0.2026237.1424
```

So `opencharly/charly/candy/plugin-{egress,init}` (old `v0.0.0` pseudo-versions) → `opencharly/plugin-{egress,init}/candy/plugin-{egress,init}` (CalVer) — the de-submodule cutover landing in the SDK pin this PR adopts. `go mod why` confirms the graph path is plugin-dsh → sdk/deploykit → plugin-{egress,init} (it is not our require).

No source change. The branch also carries a merge of `origin/main` (which already has the E-5 token fix `819f6f9`, PR #5), so the merged tree = this pin + current main. Head `8954f8b` (the pin `f9924ce` + a body re-freeze commit; the tree is unchanged from `f9924ce`).

**Why a plain `CHARLY_REPO_OVERRIDE` could not verify this one:** `plugin-dsh`'s candy is `source: builtin` — it is **compiled INTO the charly binary** (`charly/charly/charly.yml:113` `compiled_plugins:`, pinned at `charly/charly/go.mod:28`). A repo override points a remote `@github` candy ref at a local tree; it cannot swap a compiled-in plugin. So the branch was verified by building charly WITH the branch's plugin-dsh in a `go.work` workspace (below).

## How tested

**R10 — the consuming bed `check-dsh-pod` (`disposable: true`), run on a charly built with the branch's plugin-dsh.** The bed lives in `opencharly/pod-dsh` (relocated by PR #4; its schema stamp was migrated to 261 by `pod-dsh#5`).

The compiled-in branch plugin was confirmed present in the binary before the run (the token probe is a branch-only string — the published spec-232 plugin has no such line):

```
$ strings bin/charly | grep -m1 'no dsh web token'
proto3T="$(cat "${DSH_HOME:-$HOME/.dsh}/web-token" 2>/dev/null)"; if [ -z "$T" ]; then echo "no dsh web token at ${DSH_HOME:-$HOME/.dsh}/web-token"; exit 1; fi
$ ./bin/charly version
2026.268.0312
```

```
$ ./bin/charly -C <pod-dsh> check run check-dsh-pod       # disposable: true
charly check run check-dsh-pod: [image-build] PASS after 1m29.645s
charly check run check-dsh-pod: [check-image] PASS after 47.757s
charly check run check-dsh-pod: [deploy-add] PASS after 5.075s
charly check run check-dsh-pod: [config] PASS after 6.494s
charly check run check-dsh-pod: [start] PASS after 4.695s
charly check run check-dsh-pod: [bring-up-members] PASS after 85ms
charly check run check-dsh-pod: [check-live] PASS after 53.634s
charly check run check-dsh-pod: [feature-run] PASS after 30.442s
charly check run check-dsh-pod: [update] PASS after 23.939s
charly check run check-dsh-pod: [rebuild-ready] PASS after 2.81s
charly check run check-dsh-pod: [check-live-rebuild] PASS after 48.619s
charly check run check-dsh-pod: [feature-run-rebuild] PASS after 29.363s
charly check run check-dsh-pod: [cleanup] PASS after 38.807s
charly check run check-dsh-pod: [cleanup-members] PASS after 0s
charly check run check-dsh-pod: PASS (steps=14)
$ echo $?
0
$ grep 'steps:' .check/check-dsh-pod/2026.268.0551/check-live.log
36 steps: 35 passed, 0 failed, 1 skipped
```

**The one skipped step explained (block-3).** `summary.yml` for run `2026.268.0551` is `ok: true`, `errors: 0`, `warnings: 0`, `advisories: 0` (15 findings suppressed by the `dev-worktree-binary-notice` allowlist — the dev-build notice, not a defect). The single `skipped` is the nodejs candy's `pnpm install` mutating build step, skipped by DESIGN in `check live` verify-only mode:

```
  SKIP  run command=...curl ... pnpm-linux-${PNPM_ARCH} ...
  [plan:candy:github.com/opencharly/layer-nodejs:0]  skipped — verify-only mode (mutating step)
  ...36 steps: 35 passed, 0 failed, 1 skipped
```

`check live` never executes mutating `run:` steps (the mode contract); the pnpm binary's presence is still asserted by `plan:candy:...:6` (`check the self-contained pnpm binary lands on the system PATH at /usr/local/bin/pnpm` — PASS). Zero failures, zero warnings; the skip is the documented mode behavior, not an unexplained warning.

**Correction to an earlier body revision (`2 post-pin failures`).** An INTERMEDIATE run — on the stale pre-token-fix binary — failed the two web-UI probes; that was root-caused (compiled-in plugin stale; `CHARLY_REPO_OVERRIDE` cannot swap a builtin) and fixed by rebuilding charly with the branch plugin. The FINAL run above is that rebuild and has **0 failures**. The RCA for the intermediate run is retained in the rulebook section (R1).

The `dsh:` verb surface (the changed plugin's own ADE) dispatches green in-box: `dsh-verb-version` (`dsh 0.1.7-rc.1`), `dsh-verb-web-running` (the token-authenticated web UI), `dsh-verb-profile-list`, `dsh-verb-plugin-list`, and the compiled-in `charly dsh status` command.

**Fail-without-the-change (the pin's rationale).** Against the PUBLISHED plugin (spec 232), a 261 charly/project fails to resolve: the `check-dsh-pod` project (schema 261) is rejected by the plugin's `GateSchemaVersion` before the bed can run. With the branch plugin (spec 261) the project resolves and the bed passes 14/14. The `sdk`/`spec` pin is what moves the compiled spec HEAD from 232 to 261.

**Local gates (pasted, not asserted):**

```
$ GOWORK=off go build ./...   # rc=0, no output
$ GOWORK=off go vet ./...     # rc=0, no output
$ GOWORK=off gofmt -l .       # rc=0, no output
```

## Project-rulebook rule-compliance

- **R0** — `/charly-internals:plugin`, `/charly-internals:go`, `/charly-check:check`, `/charly-internals:git-workflow`, `/charly-internals:root-cause-analyzer` loaded first.
- **R1** — every anomaly carries an RCA: the intermediate 2-probe failure → stale compiled-in plugin (the builtin cannot be overridden; rebuilt charly with the branch); the lone `skipped` step → `check live` verify-only mode (documented above).
- **R2** — the whole pin change is one edit; nothing deferred.
- **R3** — one pin source; no duplication.
- **R4** — no workaround; the compiled-in plugin is verified by building charly with it (the only mechanism a builtin permits).
- **R5** — no removed identifier; the diff is pins + the graph.
- **R6** — no force-push; the branch merges current main (which carries the sibling token fix).
- **R7** — the resolved project + the `dsh:` verbs execute live; the bed passes 14/14 (pasted).
- **R8** — the emitted web-token/status artifacts are validated live in the bed.
- **R9** — the charly was built from the worktree with the branch plugin compiled in; `bin/charly version` verified.
- **R10** — a fresh `check run check-dsh-pod` including `[update]` + `[check-live-rebuild]`: PASS (steps=14), 0 failed.
- **Disposable-only autonomy** — the bed is `disposable: true`.

## Change classification

Config (dependency pin bump) in a compiled-in plugin candy, no source change. Verification gate: the `check-dsh-pod` consumer bed (`disposable: true`) driven by a charly built with the branch plugin compiled in — PASS (steps=14), fresh rebuild exercised. Attribution tier: `fully tested and validated`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*